{% extends "base.html.twig" %}

{% block title %}Modifier{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col s12 m12 l12">
                <h4>Modifier le projet {{ project.title }}</h4>
                {% if project.csv is not null %}
                    <img width="300" src="{{ asset('private/uploads/'~project.csv) }}">
                {% endif %}
            </div>
        </div>
        {% include "project/includes/project_form.html.twig" %}
    </div>
{% endblock %}